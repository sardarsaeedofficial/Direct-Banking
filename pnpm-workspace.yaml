packages:
  - "packages/*"

# Trusted dependencies allowed to run install/build scripts
# (Prisma downloads its query engine; esbuild fetches its platform binary).
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
